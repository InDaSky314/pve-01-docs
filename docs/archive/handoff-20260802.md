# Handoff — resume prompt (written 2026-08-01, for the next session)

Paste the block below into a new chat.

---

```
Continue work on the Media-Core stack (pve-01). Start by reading these, in order:

  /root/pve-01-docs/docs/lessons-learned.md          <- read this first, always
  /root/pve-01-docs/docs/npvr-epg-and-icons-20260801.md
  /root/pve-01-docs/docs/split-build-status-20260801.md
  /root/pve-01-docs/docs/dual-backend-design-20260801.md

Context: the ecosystem is split three ways — CT110 jellyfin-live (Threadfin),
CT111 jellyfin-vod, CT112 jellyfin-npvr (NextPVR), all on the Swiss VPN
(wgclient1). Production is CT105. The server and TV are on a mains power timer:
off ~22:24, on ~04:57, so all cron/EPG/maintenance is scheduled around midday.

Verify current state before acting on anything below — do not trust these
numbers, they are from 2026-08-01:

1. CT110 has never successfully recorded. Threadfin is the incumbent backend and
   is unproven on the new stack. I schedule test recordings manually.
2. VOD scan on CT111 was at 2,965 of ~236k items, resumed after the disk grew.
   Confirm it is actually progressing rather than stalled.
3. Monitoring alerts exist on CT107 (Grafana/Loki/Prometheus) but have never
   been observed firing. Force a condition and confirm the email arrives.
4. PreferredLangXMLTV=eng on CT112 is an untested change.
5. ~557 channels are still on placeholder artwork. Deliberately not pursued —
   they are dynamic event slots and obscure foreign feeds. Production has the
   same placeholder on 503 of them. Don't restart this without a reason.
6. Dashboard now requires basic auth from /etc/dvr-dashboard.auth. HTTPS was
   discussed but not implemented. Not internet-exposed.

Working preferences: research before building, confirm with me before touching
shared infra, and tell me honestly when you are uncertain rather than stating a
guess with confidence. Feel free to hand bulk mechanical work to agy
(/root/bin/agy-task.sh) — I don't use up that budget — but verify its claims
against the system rather than its report. Append anything new we learn to
lessons-learned.md.

Repos: /root/pve-01-docs is private (mirrored to InDaSky314 and nk-sys-ops).
/srv/media-core/wholphin is PUBLIC — sanitize anything that goes there.
```

---

## Notes for whoever picks this up

Every figure in the prompt is a snapshot from 2026-08-01 and some will be
stale. Check them; do not reason from them. That is the exact failure that
cost hours on the EPG — see the first entry in `lessons-learned.md`.

The lessons file is the load-bearing reference. The dated write-ups are
supporting detail.

### Where things stood at handoff

Wholphin (public) — branch `fix/jellyfin-live-tv-ts-transcode`, in sync with
`github/main`. Nothing outstanding. Fixed this session: the TS track-id crash,
the inert Direct-play-TS toggle, `ChannelFolderItem` browsing, and pausing a
released player on backgrounding.

CT112 — EPG at 997/997 channels after escaping ten raw `&` in `epg_mapping`;
439 channels with real artwork; disk grown to 40 GB.

CT110 / CT111 / CT112 — all containers were up at 23:39 CEST. Clean-shutdown
guard armed for 22:10, power reminder for 09:00, both checking both DVRs.

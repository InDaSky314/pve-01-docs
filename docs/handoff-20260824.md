# Handoff prompt — pve-01 Media-Core session continuation (2026-08-24, ~21:30 CEST)

Paste everything below into the new session as the opening prompt.

---

You are continuing an in-progress collaborative engineering session on the pve-01 Proxmox homelab (Media-Core DVR stack). The previous session ran a joint audit (Claude + "Agy") of the sports-recording pipeline, deployed fixes, wiped/reorganized the recordings library, and was mid-way through the Bayern Munich auto-record feature when it stopped. Your job: finish the remaining work, following the same protocols.

## How this project works (non-negotiable protocols)

- **Access**: everything via `sudo -n` on the host. CT105 (media-core: Jellyfin docker + Threadfin + sync scripts) via `sudo -n pct exec 105 -- <cmd>`. CT108 = EPG scrapers. Docs repo: `/root/pve-01-docs` (use `sudo -n git -C /root/pve-01-docs ...`; commits auth as root, push fans out to two GitHub remotes automatically via `push origin main`).
- **Read `/root/pve-01-docs/docs/lessons-learned.md` before any diagnostic or destructive work; append to it when you learn something new.** Highlights: container-lifecycle commands (`pct stop/reboot`, `kill -9`) are blocked by the permission classifier — ask the owner rather than working around; never open live streams/recordings on shared IPTV infra without checking the owner isn't watching TV right now; files pulled from containers land root-owned (chown before Read); `sudo -n cmd > file &` output files are root-owned 600 (check with `sudo -n cat` before diagnosing a "hang").
- **Every production change needs: a dated backup file next to the original, a README.md History-table entry with an explicit backout paragraph, the changed file synced to `/root/pve-01-docs/scripts/`, and a git commit/push.**
- **Agy** (Gemini CLI agent) is dispatched with `sudo -n /root/bin/agy-task.sh run <slug> <diagnose|plan|build|raw> "<self-contained prompt>" --timeout Nm`, ALWAYS wrapped in a background Bash call (run_in_background), never agy's own `--bg`. Reports land in `/root/agy-reports/`. **Never act on an Agy claim without independently reproducing it.** Tonight alone: its "hdhr_106 timer" claim was imprecise (API timers carry GUID ChannelIds; hdhr_* only lives in on-disk timers.json), and its "Sports/folder.jpg is the Sports Night poster" claim was flat wrong (it was legit art; the bad poster lived in Jellyfin's metadata DB). Its F-02 lineup-dump claim WAS real and was reproduced live before fixing.
- Owner communication style: research-then-build, honest uncertainty, token-conscious, wants outcomes not process narration. If you hit usage limits, pause and auto-resume — don't make the owner babysit.

## What was completed tonight (all deployed and verified)

1. **Joint pipeline audit** (my independent pass + Agy's `/root/agy-reports/packers-pipeline-audit-20260824.md`, findings cross-verified live).
2. **`/usr/local/bin/sports-dvr-auto` — deployed with all audit fixes.** Backup: `/usr/local/bin/sports-dvr-auto.pre-audit-fixes-20260824.bak`. What changed:
   - F-01: trims now require positive game-identity match on the timer name (`_timer_matches_game_identity`) + a slot/window guard — kills the remaining same-channel cross-game trim-war vector (cause of the 8/21 Packers@Broncos destruction).
   - F-02: `find_matching_program_id` post-filters returned programs on `ChannelId == channel_id` (Jellyfin silently ignores the ChannelIds filter for non-GUID ids like `hdhr_*` and returns ALL ~20k programs — reproduced live).
   - F-03/F-06: restore-stitcher no longer wedges forever when a chain timer id is missing from timers.json (disk-recovery via `resolve_active_recording_path`, terminal after end+2h), and `failed_restore_creation` chains get reconciled by timer-name adoption. Verified in dry-run against the real 8-day-stuck 8/16 Brewers chain — it immediately unwedged and reached "Ready to stitch (4 segments)".
   - F-05: stitched files are now enqueued for comskip (`enqueue_for_comskip`, appends container path to `.postprocess/queue`); `archive_segments` deletes each archived fragment's orphaned Commercial-Free cut.
   - F-07: single ESPN fetch per cycle shared by scheduler + extender.
   - F-08: `is_overlapping_timer` also checks ExternalChannelId and same-name-any-channel (channel-remap dupe guard).
   - **Bayern Munich auto-record integration**: TEAMS rows for ger.1/ger.pokal/uefa.champions (team id 132), soccer duration 2h30m, ESPN /scoreboard fallback fetch (the /schedule endpoint returns 400/0-events for soccer — known, documented in dvr-dashboard), `DYNAMIC_PPV_TEAMS`/dynamic PPV resolution ported from dvr-dashboard Phase 3 (scans provider PPV categories starting **6h before kickoff**, matches labeled slot, maps to curated "DAZN PPV NN" Threadfin channel, creates timer), `candidate_channel_ids` covers DAZN PPV + Bundesliga channels for Bayern. Verified in dry-run: "Skip VfB Stuttgart at Bayern Munich (Bayern Munich): PPV slot not expected to be labeled yet (game is 95.1h out...)".
   - `DEFAULT_SPORTS_CONFIG` gained `"Bayern Munich": True` (merges into `/var/lib/dvr-dashboard/sports-config.json` automatically on next run).
3. **Recordings wipe + library restructure (owner-authorized)**: all recordings deleted (~65GB freed) from CT105 `/srv/media-core/media/recordings/{Other,Sports,Series,Commercial Free}`, plus `.postprocess/work`, test dirs, queue truncated, `/srv/media-core/media/segments-archive` removed. Host state reset: `/var/lib/dvr-dashboard/recording-restore-state.json` and `recording-stall-state.json` = `{}`. Jellyfin "Recordings" library (ItemId `79a2726d3c50e769a8af1e4184e4fccf`): `EnableAutomaticSeriesGrouping=false`, TypeOptions with EMPTY metadata/image fetchers for Series/Season/Episode/Movie/Video (this was the "Sports Night" misfiling root cause — the mixed library TV-matched the literal folder name "Sports" to the 1998 show). Verified: 0 Sports Night series entities remain.
4. **Category artwork**: Agy-generated neon set installed (`Sports/folder.png`, `Series/folder.png`, `Tivimate/folder.png`; existing Commercial Free + Other art kept), Jellyfin refreshed, **verified via rendered contact sheet of what Jellyfin actually serves** (artwork gate: always prove art with rendered images, never counts/hashes). Contact sheet delivered to owner.
5. **Shutdown override**: extended to **2026-08-29T06:00+02:00** by writing `/var/lib/dvr-dashboard/override-until` directly (the dashboard API caps POST /api/override at 24h — and a shorter POST OVERWRITES the file, so never use the API for multi-day holds). Covers the Friday Bayern game + overnight processing. Timers `sports-dvr-auto.timer` (host) and `comskip-postprocess.timer` (CT105) are RUNNING (they were paused during the wipe and restarted).

## IN FLIGHT — resume exactly here

**dvr-dashboard edits (NOT deployed).** A scratchpad copy in the dead session had one edit; scratchpads are session-specific, so re-pull fresh: `sudo -n cat /usr/local/bin/dvr-dashboard > <scratchpad>/dvr-dashboard` and make BOTH edits:
1. Add `"Bayern Munich": True` to its `DEFAULT_SPORTS_CONFIG` (~line 138) with a brief comment — this makes the dashboard's Sports auto-recorder toggle row appear (UI enumerates the config file; `load_json_config` merges new defaults in).
2. Apply the SAME F-02 ChannelId post-filter to the dashboard's own duplicate `find_matching_program_id` (~line 509) that sports-dvr-auto got — this project keeps duplicate copies consistent (precedent: the 8/17 containment/proximity fix landed in both).
Then: backup `/usr/local/bin/dvr-dashboard` to `.pre-bayern-toggle-20260824.bak`, deploy, `py_compile`, `sudo -n systemctl restart dvr-dashboard`, and regression-verify: `curl -s -u "$(sudo -n cat /etc/dvr-dashboard.auth)" http://localhost:8099/api/sports-config` shows Bayern Munich; `/api/status` still healthy; override still 2026-08-29T06:00.

## Remaining TODO (in order)

1. Finish the dashboard work above.
2. **Baseline German-feed timer for Friday's game** (VfB Stuttgart at Bayern Munich, kickoff 2026-08-28T18:30Z = 20:30 Berlin). The English path is automated (PPV resolution from 12:30Z Friday), but the owner wants this game guaranteed: create a one-off Jellyfin timer on a German Sky Bundesliga channel as fallback. The curated Threadfin "Bundesliga" group has 34 channels (e.g. "Sky Sport Bundesliga 1 HD (720P)", tvg-chno 1001). First verify which variant exists as a Jellyfin channel AND carries EPG for the fixture (check `/LiveTv/Programs?ChannelIds=<GUID>` around Friday 18:30Z for a Bayern/Stuttgart programme; get GUID from `/emby/LiveTv/Channels` name lookup). Create via `POST /emby/LiveTv/Timers` (key at CT105 `/srv/media-core/.jellyfin_api_key`): Name **"Bayern Munich: VfB Stuttgart at Bayern Munich (German feed)"** (the name matters — the new trim guard needs identity), StartDate 2026-08-28T18:25:00Z, EndDate 2026-08-28T21:00:00Z, PrePaddingSeconds 300, PostPaddingSeconds 1800, ProgramId if a real EPG match exists. Verify it lists afterwards.
3. **Watch the first real `--apply` cycles**: `sudo -n journalctl -u sports-dvr-auto.service --since "-30min"` — confirm the Bayern skip line, Packers 8/28 dedupe ("Overlapping timer already exists"), no tracebacks. The 8/28 Packers ARI@GB timer already exists on WGBA (GUID 3f3e6cca9a2e5273f4144a5003ef719e) — leave it alone.
4. **Docs + commit**: README.md History entry (root causes, live verification evidence, explicit backout: restore each `.bak`, and for the library config re-enable EnableAutomaticSeriesGrouping via the same API POST); append lessons-learned (the hdhr_/ChannelIds-filter silent-ignore; the two wrong/imprecise Agy claims caught; the override-API 24h cap + overwrite gotcha); sync `sports-dvr-auto` + `dvr-dashboard` copies into `/root/pve-01-docs/scripts/`; commit + push.
5. **Owner-facing answers to close out** (from his 4-part request): (a) audit findings + what's now locked in; (b) cleanup done, art verified — note the client may need a library re-browse to drop cached cards; (c) Bayern: toggle shipped, source channel = English DAZN/Sky PPV slot auto-resolved ~kickoff with German Sky Bundesliga 1 as scheduled fallback; **dubbing question — answer honestly**: real-time English dubbing of a German broadcast is not practical (speech-to-text→translate→TTS would be robotic, unsynced, GPU-heavy); the right answer is the English feed the pipeline now hunts automatically. (d) Note Friday-evening concurrency: German fallback + possible English PPV recording + owner possibly watching live = up to 3 concurrent provider streams; provider has hard concurrent-connection limits (proven 2026-08-07) — flag it and let the owner decide whether to drop the German fallback once the English slot is confirmed recording.
6. **Friday (Aug 28)**: verify around 12:30Z that PPV resolution attempts appear in the journal, and near 18:00Z that a timer exists (English and/or German). After the game: confirm recording, stitching (if stalls), comskip enqueue of any stitched file — this is the first live test of tonight's fixes.

## Key paths
- `/usr/local/bin/sports-dvr-auto` (+ `.pre-audit-fixes-20260824.bak`), timer every 5 min with `--apply`
- `/usr/local/bin/dvr-dashboard`, service `dvr-dashboard`, auth `/etc/dvr-dashboard.auth`, port 8099
- `/var/lib/dvr-dashboard/`: sports-config.json, override-until, recording-*-state.json, dvr-automation-events.jsonl
- CT105: `/srv/media-core/media/recordings/`, `/srv/media-core/comskip/process-queue.py`, `/srv/media-core/.jellyfin_api_key`, Threadfin xepg `/srv/media-core/threadfin/conf/xepg.json`
- Provider (Xtream): `http://cf.teltv.xyz` user `c21fa33845e9` pass `3e110912e1`; PPV categories 573/575/1441/1811/1137/433
- Agy reports `/root/agy-reports/`; Bayern design doc `docs/bayern-bundesliga-dvr-design.md`; trim-war incident `docs/incident-20260822-packers-broncos-trim-war.md`

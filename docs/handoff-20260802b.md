# Handoff — 2026-08-02 afternoon

Supersedes `handoff-20260802.md` (written 2026-08-01). Read
`lessons-learned.md` first, as always.

## Resume prompt

```
Continue work on the Media-Core stack (pve-01). Read in order:

  /root/pve-01-docs/docs/lessons-learned.md          <- always first
  /root/pve-01-docs/docs/handoff-20260802b.md        <- this file
  /root/pve-01-docs/docs/channel-icons-runbook.md

Verify before acting. Every number below is from 2026-08-02 midday.

Immediate: check /root/agy-icons-run.log. An unattended agy run was
generating channel logos in batches of 6. Look at the artwork before
installing any of it -- agy exits 0 with files present and the artwork
still unusable. Contact sheet:

  magick montage /root/agy-icons/*.png -tile 6x4 -geometry 150x150+4+4 \
    -background '#222' /tmp/sheet.png
```

## What changed today

**Grafana alerting was completely dead and now works.** The Alertmanager
config had been unparseable since ~2026-08-01, so no rule could deliver
anything while every instance sat at `Normal`. Two invalid constructs, both
written directly into `grafana.db` by SQL, bypassing the validating API: a
mute window wrapping midnight (`22:20-05:10`), and `mute_time_intervals` on
the root route. Restored the last good config, re-applied the mute timing via
the provisioning API split into `22:20-24:00` + `00:00-05:10`, both pinned to
`Europe/Berlin`. **Owner confirmed the test email arrived** — first end-to-end
delivery ever. Backup: `grafana.db.bak-alertfix-20260802`.

**CT 112 channel icons: 997/997, verified rendered.** Root cause of "icons not
updating" was 997 orphan `BaseItemImageInfos` rows pointing at posters deleted
by an `rm -rf metadata/*` — Jellyfin believed it had the images and never
re-fetched. 83 real logos sourced from tv-logos and installed; verified
byte-identical from Jellyfin, not just at the NextPVR API.

**EPG propagation to CT 112.** `epg-sync-ct112.timer`, daily 12:28 — between
CT 105's 12:01 sync/12:25 xepg and NextPVR's 12:35 ingest. Refuses to publish
a file that does not parse as XML.

**Shutdown guard and power reminder now see every stack.** Both were hardcoded
`CT = "105"`, so CT 112 — the only stack that had ever recorded — was invisible
to both. Now iterate `STACKS`. Proved in both directions with a real recording;
the old guard said "shutting down cleanly" over it.

**CT 110 (`jellyfin-live`) retired.** Stopped, `onboot=0`. Also removed from
`stack-monitor.py` targets, because it reports `epg_age_hours = 999.0` when it
cannot stat the file — leaving it in would have fired two alerts every cycle
for a chosen outage. Revival steps in `split-build-status-20260801.md`.

**Weekly lineup + movie report.** `lineup-watch.timer`, Sundays 12:40. Query
runs inside CT 105 (Swiss egress, which the provider account requires); mail
sends from the host. Silent when nothing changed. Every report embeds a
ready-to-paste prompt for acting on it.

## In flight

**Icon generation — finished, and it lost most of its output.** All 25 batches
ran and every one logged `6/6`. The working directory ended with **46 files of
~148**: `fast-01` through `fast-17` are gone, and every survivor is timestamped
12:54 or later. Something in a later agy session cleared `/root/agy-icons`
mid-run. I did not identify the exact command — the batch reports do not show
an obvious `rm` — so treat the cause as unconfirmed.

The 46 survivors are preserved in **`/root/agy-icons-keep/`**.

`run-batches.sh` now copies each batch into that keep directory immediately
after it completes, so a later session cannot destroy earlier work, and logs a
running kept-total alongside the per-batch count.

**This is the sharpest instance today of the standing lesson**: 25 consecutive
`6/6` results, every one true at the moment it was measured, and the aggregate
still wrong. Per-item checks do not add up to a whole-run check.

To finish the remaining ~102, re-run `/root/run-batches.sh` after removing the
`fast-01` skip line, then verify `ls /root/agy-icons-keep/*.png | wc -l`
against the 148 expected — not the per-batch log.

Nothing generated has been installed. Install path is documented in
`channel-icons-runbook.md`; the crucial detail is that clearing a *subset* of
Jellyfin's channel image rows does not cause a re-fetch — clear all of them and
run one guide refresh to completion.

Treatment groups (`/root/agy-batches/`):
- `fast-*` 120 channels, original artwork, genre palette
- `sports-*` 17, modernise retired brands (CSN -> NBC Sports, Bally ->
  FanDuel) keeping the region legible
- `affiliate-*` 11, network-inspired but not copies
- 40 provider-invented pools (`BBC STREAM n`, `HBO MAX ORIGINAL n`,
  `BIG BROTHER n`) deliberately left alone — owner is happy with them

## Not started

**Lineup renumbering.** Owner wants the 40 provider pools moved to the end of
the lineup. Do it in `live_selections[].start_chno` in
`/srv/media-core/sync/config.json` on CT 105 — not by hand, or the 12:25
renumber overwrites it.

Two things to know first:
- **`config.json` on CT 105 drives the whole estate.** There is one lineup;
  CT 112 consumes the same generated playlist. This is a production change.
- There is a **pre-existing block overflow**: the sync logs `group 'German
  Cable & Entertainment' overflows its block (reaches 2584, next block starts
  at 2550)`, and that group appears twice with different `start_chno` (2545
  and 2550). Fix that in the same pass rather than stacking moves on top.

**No Wisconsin CW exists.** The owner asked to place Wisconsin CW channels
beside their cities; the provider carries none (all 10 CW channels are other
markets). Nothing to do unless the provider adds one — which the weekly report
would now catch.

## Standing facts worth not re-deriving

- Egress: CT 105/111/112 all exit **Zürich `156.146.62.42`**. The **host**
  exits **US** (`147.124.195.73`, Tier.Net). That is by design. The provider
  account expects the Swiss IP — run provider queries inside CT 105.
- Provider API needs `User-Agent: MediaCoreSync/1.0` or it 403s, and
  intermittently answers 200 with an empty list; retry that like a transport
  error.
- Jellyfin API key on CT 112: `claude-iconfix`. Delete it when done.
- Dashboard: `http://pve-01.tail8f3e6.ts.net:8099/`, user `dvr`, creds in
  `/etc/dvr-dashboard.auth`.

## On agy

Mixed, and the pattern is consistent: **fine at small mechanical batches,
unreliable at anything long, and its self-reports cannot be trusted.**

- Two runs of the logo-research task exited **0 with nothing on disk**, dying
  mid-task while emitting a long script ("stream cut out"). The matching was
  done directly instead (`scripts/icon-match.py`).
- Image generation works — `generate_image` fires. Nano Banana models are not
  in `agy models` because they are backend tools, not selectable session
  drivers.
- Batch of 12 produced cropped wordmarks and invented gibberish. Batch of 6 is
  clean. **Always look at the artwork.**
- `agy-task.sh` was patched so display-name models (`Claude Opus 4.6
  (Thinking)`) dispatch; previously only hyphenated slugs worked.
